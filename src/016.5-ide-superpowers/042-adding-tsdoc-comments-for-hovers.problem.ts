/**
 * Adds two numbers and returns their sum
 * @param a number to add to
 * @param b number being added
 * @returns product of two numbers
 */
const myFunction = (a: number, b: number) => {
  return a + b;
};

myFunction(2,4);
